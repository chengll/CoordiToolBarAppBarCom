<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <!--定义头部布局-->
    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">
        <!--实现悬停的层叠效果
         当CollapsingToolbarLayout中设置了layout_scrollFlag:属性时，它有3个值分别是：
         1.scroll:想滚动就要设置这个值
         2.enterAlways:实现快速返回，当往下拉悬停的布局时，能让上面隐藏的布局立马显示出来，
         3.exitUntilCollapsed:向上滚动时收缩View，但可以让TOOLBar固定在上面

         collapsingToolbarLayout的其他属性：
         1.contentScrim:当CollapsingToolbarLayout全部折叠后的背景颜色
         2.expandedTitleMarginStart:设置还没有伸缩时标题向左填充的距离
         3.layout_collapseMode(折叠模式：有2个值：)
            1）pin:设置为这个模式，当CollapsingToolbarLayout完全收缩后，ToolBar还可以保留在屏幕上
            2）parallax:设置为这个模式时，在内容滚动时，CollapsingToolbarLayout中的布局（比如ImageView）
               也可以实现滚动效果，通常和layout_collapseParallaxMultiplier(设置视差因子)搭配使用。
        -->
        <android.support.design.widget.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fitsSystemWindows="true"
            app:contentScrim="?attr/colorAccent"
            app:expandedTitleMarginEnd="64dp"
            app:expandedTitleMarginStart="48dp"
            app:layout_scrollFlags="scroll|enterAlways">
            <ImageView
                android:id="@+id/backdrop"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:layout_margin="5dp"
                android:src="@mipmap/ic_launcher"
                android:fitsSystemWindows="true"
                app:layout_collapseMode="parallax"
                android:scaleType="centerCrop"
                />
           <android.support.v7.widget.Toolbar
               android:id="@+id/toolbar"
               android:layout_width="match_parent"
               android:layout_height="?attr/actionBarSize"
               android:background="?attr/colorPrimary"
               app:layout_scrollFlags="scroll|enterAlways"
               app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
               app:theme="@style/ThemeOverlay.AppCompat.ActionBar"
               app:title="我是标题"
               />
        </android.support.design.widget.CollapsingToolbarLayout>

        <!--tablayout-->
        <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabGravity="fill"
            app:tabIndicatorColor="#ff6699"/>

    </android.support.design.widget.AppBarLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floating_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:src="@mipmap/ic_launcher"
        android:layout_margin="@dimen/floating_label_text_size"/>

</android.support.design.widget.CoordinatorLayout>
